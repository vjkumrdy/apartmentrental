<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.findinganapartment">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SlashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity"></activity>
        <activity android:name=".activities.RegistrationActivity"></activity>
        <activity android:name=".activities.LandLordDashboardActivity"></activity>
        <activity android:name=".activities.AdminDashboardActivity"></activity>
        <activity android:name=".activities.AdminLoginActivity"></activity>

        <activity android:name=".activities.MainActivity"></activity>
        <activity android:name=".activities.AddPropertyActivity"></activity>
        <activity android:name=".activities.ForgotPasswordActivity"></activity>
        <activity android:name=".activities.EditYourProfileActivity"></activity>
        <activity android:name=".activities.AllPropertiesActivity"></activity>
        <activity android:name=".activities.AdminEditProfileActivity"></activity>
    </application>

</manifest>